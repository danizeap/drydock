---
description: Merge a change's delta specs into the living capability specs
---
Sync delta specs for the SDD+ change: $ARGUMENTS

Use the `spec-sync` skill. If no change name was given and it cannot be inferred from context, list changes that have `specs/` deltas and ask — never auto-select. Apply ADDED/MODIFIED/REMOVED/RENAMED intelligently, preserve unmentioned content, summarize per capability.
