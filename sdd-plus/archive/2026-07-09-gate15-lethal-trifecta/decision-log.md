# Decision Log

## Change

gate15-lethal-trifecta

## Decisions

| Date | Decision | Reason | Alternatives Considered |
| --- | --- | --- | --- |
| 2026-07-09 | Re-scope Gate 15 to a structural lethal-trifecta test rather than adding a new gate | The threat is a combination of capabilities Gate 15 already owns (AI/agent security), not a new domain; a combination-check fixes the "tick each item, miss the combination" failure | A new Gate 22 "Agent Exfiltration" (rejected: fragments the AI-security concern, renumbers the framework); leaving Gate 15 as-is (rejected: the CONFIRMED finding shows it passes the vulnerable config) |
| 2026-07-09 | State explicitly that RLS and read-only mode do NOT break the trifecta | The single most counter-intuitive, most load-bearing fact from the verified research (service_role bypasses RLS; read-only still permits read+injection+exfil) — a non-expert's whole mental model of "I turned on database security" is exactly the false comfort | Omitting it (rejected: the reviewer would tick "RLS on" and pass the vulnerable config — the precise documented failure) |
| 2026-07-09 | Make the trifecta a recordable artifact (three boolean legs in the applicability template), not only prose | Aligns with the project-wide "evidence over prose" philosophy; a durable record that the combination was checked, and the shape a future CLI auto-detector can fill | Prose-only in the framework spec (rejected: no durable per-project evidence; nothing to challenge or render) |
| 2026-07-09 | Break-a-leg is the prescribed control, framed as "remove any one of the three" | Matches the structural nature of the threat: you cannot patch prompt injection, but removing any single leg neutralizes the combination — an actionable, non-expert-legible instruction | Listing point mitigations without the unifying frame (rejected: leaves the non-expert without a clear "what do I actually do") |
| 2026-07-09 | Scope strictly to Gate 15; Gate 10/CI-CD/RLS changes are separate packets | Keeps the packet small, reviewable, and tied to the one CONFIRMED (3-vote) finding; the other recommendations are strongly-sourced-but-verification-incomplete and deserve their own evidence framing | One big v0.4 packet bundling recommendations 1-4 (offered to the Owner; the Owner chose Gate-15-first) |
| 2026-07-09 | New living capability spec `launchguardian.md` whose Purpose points at the framework spec, rather than duplicating gate content | Satisfies the archive sync gate and gives LGF a living capability record, while keeping the framework spec the single source of gate detail (avoids drift) | Writing full gate content into the capability spec (rejected: duplicates and will drift from the framework spec) |
| 2026-07-09 | Trifecta applicability fields are tri-state strings (`private_data_access`/`untrusted_content_exposure`/`outbound_channel` = true/false/unknown) plus `broken_leg` + `mitigation` — not booleans or a machine `legs_present`/derived-risk field (supersedes the plan/brief's earlier wording) | Matches the gate-level `applies` tri-state style already in the templates and captures "unknown"; the derived risk (all three legs + no broken leg = high-risk) is stated normatively in the framework spec, so the YAML stays a human-filled record, not a rules engine | Booleans + a machine-computed derived-risk field (rejected: templates are records, not an engine; tri-state matches existing style) |
| 2026-07-09 | Verifier VERIFIED WITH NOTES; resolved all four notes before archive | Faithfulness is the highest bar for a security framework: restored the confirmed `integration_tokens`/OAuth detail to the research note (it was dropped when summarizing the [3-0] claim) so the framework's most specific detail is sourced; calibrated the read-only clause from asserted fact to stated mechanism; added the worked trifecta example to the output template; reconciled the field-shape wording (this row) | Shipping with the notes open (rejected: an unsourced/over-confident claim in a security spec is the exact failure this gate exists to catch) |
