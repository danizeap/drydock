# Decision Log

## Change

owner-brief

## Decisions

| Date | Decision | Reason | Alternatives Considered |
| --- | --- | --- | --- |
| 2026-07-07 | The unit of the Owner surface is the commitment (promise-ladder rung per work item), not the change diff | Answers the Owner's four wake-up questions from state Drydock already computes deterministically; "built but not yet checked" is the state the enforcement layer exists to expose | Change-log style listing (engineer-shaped); model-narrated summary (unfalsifiable, drifts into vibes) |
| 2026-07-07 | Hooks never write OWNER_STATUS.md; the file is written only by the explicit /drydock:brief path | Session-orientation spec pins the project tree read-only (adversarially derived); Owner commits out-of-band between turns (dirty-tree churn); a failed Stop-hook write is invisible = silently lying dashboard | SessionStart regeneration; Stop-hook regeneration; both rejected on three invariants |
| 2026-07-07 | Engine is plugin-only `scripts/brief.py`, not an sdd.py subcommand | sdd.py is dual-copy (11th check_sync pair forever) and version-skews: scaffolded projects keep old copies while the plugin advances; hooks/_drydock_common already holds every needed primitive | `sdd.py brief` (rejected: sync tax + skew + find_root sys.exit landmine); separate scaffolded script (same tax) |
| 2026-07-07 | Facts deterministic, language model-constrained: engine emits FACTS block; command may only translate it | A model that can add facts will eventually add false comfort; absence-as-absence vs absence-as-zero must be decided where a test can pin it | Pure-prose command reading raw files (confabulation risk); fully deterministic prose (unreadable for non-experts, no language adaptation) |
| 2026-07-07 | Event ledger is per-user, per-project, category-only NDJSON appended best-effort by existing verdict paths | "What the safety net did" has no data source today; categories-only keeps secrets/paths out of a cache file; per-call swallow-all keeps guard polarity intact | Logging full commands/paths (privacy leak into a world-readable-ish cache); a new PostToolUse observer hook (new per-call latency for pure telemetry); no ledger (flagship section confabulates or renders empty) |
| 2026-07-07 | Ledger phrasing rule: "paused N actions until a change plan existed", never "prevented N disasters"; liveness probe events are excluded from prevention counts | packet_guard denies are deliberately recoverable; the orientation probe fires synthetic blocks every session — counting either as danger manufactures anxiety and is false | Raw deny counts (false drama); omitting prevention entirely (loses the trust asset) |
| 2026-07-07 | Owner-line capture lives in commands/new.md prose + engine fallback to User Need first paragraph; sdd.py templates untouched | Touching sdd.py's embedded template re-opens dual-copy + version skew for one line; the fallback covers every existing packet | Editing sdd.py brief template (rejected this packet; revisit when sdd.py next changes anyway) |
| 2026-07-07 | Two delta specs only: new `owner-brief` capability (owns the ledger contract incl. which hooks append) + MODIFIED `session-orientation` (sentinel) | The guards' verdict behavior is deliberately unchanged — that is the ledger contract's core promise — so their capability specs stay untouched; the contract lives with the capability that owns the ledger | Five deltas touching every guard spec (adds sync surface without adding meaning; precedent: cross-hook writer contract lives in completion-gate's spec) |
| 2026-07-07 | Vocabulary rules are spec'd: never surface BLOCKED/NOT VERIFIED/packet/mode/skill names; "waiting on your decision", "done & documented", caught-and-fixed narrated past-tense with resolution | Internally-correct states read as emergencies to non-experts; unresolved alarm without an action is forbidden even when true | Raw framework vocabulary (panic); euphemism without provenance (false peace — the death condition) |
| 2026-07-07 | brief.py authors OWNER_STATUS.md itself (frozen en/es label sets, deterministic bytes); the model renders only the ephemeral chat brief; packet_guard always-denies freelance writes to the basename | Red-team critical: a model-authored durable file + .md exemption meant any agent could hand-write a green status (even with a copied fingerprint); deterministic authorship is golden-testable, the deny closes the freelance path | Model writes the file under advisory rules (tier-1 only — forgeable); no file at all (loses the async/GitHub surface) |
| 2026-07-07 | brief.py gets its own ascent-requires-positive-evidence rung assigner with a closed PASS grammar; the shared claimed-done parsers are never reused for the brief | Red-team critical, live-confirmed: the completion gate's parsers deliberately fail toward silence, which for the brief's polarity fails toward false peace — a forged one-line "PASS." and even "NOT VERIFIED" both read as filled; missing tasks.md reads as claimed-done | Reusing packet_state/verification_result_is_pending (wrong polarity); tightening the shared parsers (would change completion-gate semantics shipped in 0.2.1) |
| 2026-07-07 | "Independently checked" never renders from typeable state: rung is "checked & recorded" (PASS grammar), caption upgrades to "confirmed on this computer" only via a verify-run ledger event whose packet content-hash matches current | The verifier's authorship is byte-indistinguishable in verification.md; the only unforgeable signal is a deterministic gate run recorded outside the repo; post-verify edits must demote automatically | Trusting the Result text (laundering); signing/HMAC schemes (key management for no real gain — the agent could still run the signer) |
| 2026-07-07 | Probe exclusion is mechanical: session_orient sets DRYDOCK_PROBE=1 in probe child env; append_event no-ops under it; verdict paths never read it | The liveness probes fire synthetic denies every session — without exclusion the flagship count fabricates ~2 events/session; heuristic subtraction would corrupt real counts on degraded sessions | Counting probes (false drama); subtracting 2 per session (wrong on degraded paths); distinguishing by payload shape (fragile) |
| 2026-07-07 | Ledger appends never fsync, fire only on non-silent verdicts, and are the final statement after the verdict bytes are flushed | Swallow-all catches exceptions, not hangs: a slow fsync at the hook-timeout tail would kill an exit-2 guard and fail it OPEN; the ledger is a disposable cache — durability buys nothing worth that risk | fsync for durability (rejected); PostToolUse observer hook (new latency on every call for telemetry) |
| 2026-07-07 | OWNER_STATUS.md git handling is the Owner's one-time explicit choice (commit-and-it-lags vs .gitignore-and-it's-private), offered at first write; no-change runs never rewrite the file | Both defaults are wrong for someone: churn + permanently-stale GitHub copy vs dead visibility; the design must not decide silently, and deterministic authorship makes the no-op short-circuit exact | Always commit (stale public copy); always ignore (kills the async surface); deciding in init-project (user hasn't seen the file yet) |
| 2026-07-07 | Status-file layer ships frozen en + es label sets, recorded as lang=<code> in the fingerprint comment and reused on regeneration; chat rendering stays any-language | The durable file must be deterministic (golden-testable) yet readable by the non-English Owners the product targets; recording the language kills flip-flop churn | English-only file (excludes the target demographic); model-translated file (non-deterministic bytes, forgeable); full i18n framework (theater) |
