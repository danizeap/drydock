# Tasks

## Change

audit-debt-polish

## Implementation

- [x] Recover exact audit findings from the transcript; confirm which are already resolved on disk.
- [x] Rename `explore` skill to `explore-mode` (frontmatter, command reference, `git mv`).
- [x] Swap the skill id in all routing tables + README skill list; grep-verify no orphaned refs.
- [x] Add `tests/test_skill_command_registry.py` collision + name/dir guard.
- [x] README: accurate 5-hook safety layer + "It governs itself" autonomy story.
- [x] Graduation notes on `architect` and `mcp-ranger`.
- [x] Reconcile deployment routing to the Owner's `backend`-owns decision across AGENTS (×2) + operator guide.
- [x] Correct hook count in both `CLAUDE.md` copies.
- [x] Run verification (full suite, check_sync, rename sweep, verifier subagent).
