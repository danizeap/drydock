# Tasks

## Change

codex-host-mvp

## Architecture and Approval

- [x] Confirm project context, FULL mode, standards, and skill routing.
- [x] Prove Codex plugin hook trust, coverage, upgrade, nested, and Windows
  behavior.
- [x] Probe the local Claude CLI authentication and structured failure
  contract.
- [x] Prove native agent freshness, project-scoped model routing, permission
  inheritance, and separate-process verifier isolation.
- [x] Write the Codex host Build Blueprint and delta specs.
- [x] Obtain a Claude architectural peer review; record its non-converged
  blockers without treating them as automatically true.
- [x] Reproduce the worktree omission, integrity race, and unsafe dispatcher
  reuse against the repository.
- [x] Amend the blueprint and delta specs with worktree containment,
  verify-and-execute-the-same-bytes, host-native state machines, fail-closed
  tool mapping, pace governance, and honest isolation claims.
- [x] Obtain Claude's second architectural review; confirm its new finding that
  nested permission inheritance makes asserted worktree containment
  unenforceable.
- [x] Prove from a `danger-full-access` parent that a separate fixed-root
  `workspace-write` Codex process can write inside its disposable worktree
  while the sandbox denies a sibling Owner-checkout write; independently
  verify fingerprints and clean up.
- [x] Amend the packet with separate-process mutation, exclusive worktree
  leases, narrow hook matchers, CI bundle-sync, and a reproducible plain-source
  bundle format.
- [x] Owner logs the local Claude CLI in; `auth status` reports an active
  first-party Claude Max session.
- [x] Owner accepts Claude Opus 5 as the Phase 0 peer fallback when Fable is
  unavailable.
- [x] Prove the authenticated CLI reaches the exact `claude-opus-5` model with
  tools disabled, low effort, no persistence, no fallback, and bounded process
  cleanup.
- [x] Prove a bounded schema-valid Phase 0 Claude peer transport round trip.
  A minimal exact-Opus-5 two-turn probe returned exit 0, `is_error: false`, and
  the required structured envelope. This proves the adapter contract, not
  architectural convergence; the high-effort full-packet call separately
  reached its 180-second process deadline without emitting a result.
- [x] Claude re-reviews the corrected packet and returns convergence with no
  blocking concerns; reconcile its non-blocking lease, Git-ownership, cleanup,
  and evidence requests into the packet.
- [x] Owner approves the corrected blueprint and MVP boundary before
  implementation.

## Implementation

- [x] Correct public enforcement claims and freeze adapter contracts.
- [x] Keep the first vertical slice inside new Codex-owned paths; prove the
  protected Claude/lifecycle surfaces remain byte-compatible.
- [x] Add the self-contained Codex plugin shell, lifecycle skills, deterministic
  project-scaffold bundle/init path, and readiness reporter.
- [x] Add and test the self-contained, verify-once/execute-in-memory Codex
  enforcement adapter.
- [x] Add and test the separate-process fixed-root mutation runner, exclusive
  worktree leases with bounded stale/dead-process reclamation, runner-owned Git
  metadata operations, orphaned-lease cleanup, Owner-state drift checks, and
  project advisory-agent scaffolding.
- [x] Add and test the separate-process read-only verifier.
- [x] Add and test the host-neutral controller and Claude peer adapter.
- [x] Owner approves the bounded LaunchGuardian/CI remediation slice.
- [x] Pin `actions/checkout` and `actions/setup-python` to verified immutable
  SHAs without changing their current major-version behavior.
- [x] Remove the non-executable plain-WebSocket documentation false positive
  without changing the historical transport claim.
- [x] Rerun strict LaunchGuardian and reconcile the exact remaining findings
  without suppression or waiver.
- [x] Owner approves recording the two exact Semgrep Python-3.6 compatibility
  rule IDs as `not_applicable` using the documented Python-3.9+ boundary.
- [x] Add the exact reviewed dispositions without wildcard, path, or inline
  scanner suppression.
- [x] Rerun the strict five-scanner gate through the reviewed LaunchGuardian
  source build and reconcile status, raw errors, and remaining findings.
- [x] Pin the LaunchGuardian companion repository and distributed-template
  Action refs to verified full SHAs, add a mutable-ref regression, and rerun
  its clean-source strict self-scan.
- [x] Install and trust the local Codex plugin and capture the fresh Desktop
  SessionStart liveness record without treating the broken readiness report as
  a pass.
- [x] Make readiness resolve the exact current Desktop task and unique plugin
  data record automatically while failing non-positively on missing,
  mismatched, or ambiguous evidence.
- [x] Reinstall the current corrected local plugin and repeat fresh-task
  readiness plus selective allow/deny dogfood against its exact handler
  revision.
- [x] Bind readiness to an explicit CLI hook probe and bounded recent supported
  `PreToolUse` activity marker; reject out-of-window resume replay and linked
  candidate paths; disclose unsigned plugin-data evidence and in-window replay.
- [x] Independently review the post-convergence readiness correction and its
  live dogfood evidence.
- [x] Reconcile the integrated dogfood completion claim with durable proof,
  verifier, integration, and remote evidence; a task checkbox alone is not
  completion evidence.
- [x] Dogfood one integrated end-to-end Codex-hosted workflow using the route
  the Owner selected before workflow start. The default route retains plan peer
  agreement and cross-review. The explicit Codex-only `single_pilot` route
  omits both peer phases, spends no Claude quota, and records
  `peer_convergence: not_established`. Both routes still require isolated
  mutation, exact-fingerprint full proof, candidate-bound LaunchGuardian,
  separate read-only verification, deliberate integration, and an authorized
  non-force push. Component-level runner, verifier, readiness, and live-hook
  probes do not complete this task by themselves.
- [x] Add a candidate-bound, fail-closed LaunchGuardian phase between proof and
  independent verification for every mutating Codex workflow.
- [x] Prove missing, timed-out, malformed, stale, disabled/incomplete-scanner,
  invalid-LGF, and open-blocker security evidence cannot admit verification,
  integration, or push.
- [x] Remove tar extraction from fresh proof materialization: validate the Git
  archive against the committed tree, then write only verified blob bytes and
  modes without an in-root archive filename collision.
- [x] Review and independently verify the operational-core security slice under
  the Owner-selected route. The default route retains peer cross-review. The
  explicit Codex-only route omits peer review and must not claim cross-model
  agreement or epistemic independence. Candidate-bound LaunchGuardian and a
  separate read-only verifier remain mandatory. Publishing, release, and
  archiving remain outside this task.
- [x] Update user/operator documentation and project context.
- [x] Run full verification, independent review, and LaunchGuardian.
- [x] Hotfix the inline Codex hook verifier to preserve raw UTF-8 stdin bytes,
  retain structured `PreToolUse` integrity denials, regenerate the trusted
  definition, and record focused proof without changing runtime or living specs.
- [x] Make all six Codex orchestration stdin entry points read one bounded raw
  byte stream and decode strict UTF-8 before workflow, provider, runner, or
  integration dispatch; prove Windows-locale, malformed, and oversized cases.
