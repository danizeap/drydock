# Decision Log

## Change

privileged-test-runner-hardening

## Decisions

| Date | Decision | Reason | Alternatives Considered |
| --- | --- | --- | --- |
| 2026-07-25 | FULL mode; `mcp-ranger` primary with backend, testing, and LaunchGuardian support | This changes a CLASS 3 local process surface that executes after model-authored code; a superficially small subprocess edit changes permissions and release evidence | Treat as a scanner-only cleanup; suppress the finding; fold it invisibly into `codex-host-mvp` |
| 2026-07-25 | Compile and resolve the whole test plan before Codex discovery or worktree creation | Validation after delegation still lets an unsafe request spend quota/create state, while executable lookup after delegation lets a worker plant the program that the trusted runner launches | Validate only inside `run_tests`; rely on command-shape trust after execution |
| 2026-07-25 | Structured absolute argv is authoritative; legacy strings are a bounded compatibility adapter | It removes implicit shell grammar while keeping ordinary `pytest -q`, `git --version`, and safe `&&` workflows usable. JSON argv gives Windows an unambiguous quoting path | Delete `--test-cmd` immediately; retain unrestricted strings; named profiles only |
| 2026-07-25 | Refuse directly selected known shell launchers and Windows batch/command shims | `shell=False` is not a truthful top-level no-shell claim if argv directly launches a known shell or Windows dispatches a batch file through `cmd.exe`. This decision does not claim transitive prevention and is narrowed explicitly below | Permit them because the argv is structured; maintain an incomplete metacharacter blacklist |
| 2026-07-25 | Initially, do not claim repository test-code containment from `shell=False` alone | Absolute argv closes shell injection and worktree executable search, but direct execution still gives project code controller permissions. The later `codex sandbox` decision supersedes direct execution while retaining a narrower native-Windows read disclosure | Call `shell=False` a complete execution sandbox; silently stop running tests |
| 2026-07-25 | Run approved tests through the model-free `codex sandbox` command and fail closed if it is unavailable | A shell-free argv still lets worker-authored test code inherit the controller's write and network authority. The installed Codex sandbox denied writes outside the assigned root, denied the same action from a child process, and denied a direct socket | Execute directly as controller; require Docker; postpone all tests |
| 2026-07-25 | Record point-in-time write/network containment evidence, not native-Windows read isolation or per-run verification | On this machine, the custom profile denied outside writes and direct sockets but could read the Owner checkout even with `:root` and exact-path deny rules; the nonzero first read probe was only an output-encoding error after a successful read. The mechanism and one probe do not back a stronger or per-run claim | Report full filesystem isolation from documentation alone; discard the useful point-in-time evidence |
| 2026-07-25 | Keep the active Codex home but pin the elevated Windows backend, explicit permission profile, environment reductions, network denial, and managed constraints on the CLI | A fresh isolated Codex home could not reuse the already-installed elevated sandbox and failed readiness. On the active home, a hostile project config requesting both legacy and profile danger-full-access still failed an outside write because the explicit `-P` profile remained authoritative on this alpha build. The remaining Owner-home dependency is disclosed and readiness is point-in-time | Inherit all Owner/project config silently; claim an unproven isolated home; fall back to unelevated sandbox |
| 2026-07-25 | Accept the first independent review as non-converged and narrow the shell boundary to direct known launchers | The reviewer demonstrated that `env sh -c ...` and many interpreters can transitively launch a shell despite the direct-launcher denylist. The sandbox bounds the side effects, but no finite denylist can establish a transitive no-shell property | Keep the broad SHALL; expand the denylist indefinitely; restrict all projects to a small test-executable allowlist |
| 2026-07-25 | Keep broad project test compatibility and advise on known indirection/interpreter launchers | An allowlist would be a stronger boundary but would make a framework runner unusable across legitimate stacks. The honest contract is that Drydock pins the top-level executable and refuses direct known shells; it cannot prove what an allowed executable runs internally | Project-specific executable allowlists; no advisory |
| 2026-07-25 | Attempt bounded tree termination on timeout and report its evidence and limitations | `subprocess.run(..., timeout=...)` only guarantees handling of the direct child. POSIX process groups and Windows `taskkill /T` improve cleanup, but descendants that deliberately escape those mechanisms cannot be claimed absent | Leave orphan cleanup undisclosed; claim perfect descendant containment; add a Windows-only Job Object dependency |
| 2026-07-25 | Report write scope and direct-network denial as requested configuration, not achieved per-run facts | The runner constructs those restrictions and a point-in-time live probe observed enforcement, but an ordinary test result performs no hostile boundary probe | Preserve static achieved-sounding labels; run destructive boundary probes during every project test |
| 2026-07-25 | Accept the second independent review as converged with no blocking concerns | Claude inspected the remediated code, reproduced the complete `642 passed, 8 skipped` result, and verified closure of the direct-shell overclaim, timeout-descendant gap, and achieved-sounding metadata gap. The exact JSON is preserved in `peer-review-round-2.json` | Treat `converged: true` as release approval; omit the review's non-blocking gaps and risks |
| 2026-07-25 | Preserve the peer-reviewed code and carry four non-blocking refinements into the release track | Windows `taskkill /T` is weaker than a Job Object for breakaway descendants; interpreter advisories may become noise; cleanup can add a bounded margin after the execution deadline; native-Windows host reads remain open. None defeats the current disclosed boundary, and changing code now would invalidate the exact independent review | Implement every refinement immediately and require a third review; discard them because the reviewer converged |
| 2026-07-25 | Do not apply the suggested “skip `killpg` after the direct child is reaped” change | POSIX process groups can outlive their leader, and the current code checks group liveness immediately before `SIGKILL`. Skipping the group signal when the direct child exits could strand the very descendants cleanup is intended to reap. A theoretical check-to-signal race remains a follow-up, not a proven blocker | Adopt the suggestion verbatim; claim process targeting is race-free |
