# Decision Log

## Change

codex-host-mvp

## Decisions

| Date | Decision | Reason | Alternatives Considered |
| --- | --- | --- | --- |
| 2026-07-24 | PROPOSED: Codex is the pilot through a new host adapter; the current conductor is not the shared core | The current conductor is a Claude→Codex callee adapter and hardcodes Codex discovery, models, safety flags, and roles | Literal seat swap; shipping `scripts/conductor/*` as-is |
| 2026-07-24 | PROPOSED: epistemic equality, asymmetric control | Claude can object and block convergence, but Codex owns permissions and side effects; unresolved disagreement returns to the Owner | Symmetric host authority; treating Claude as a disposable one-shot oracle |
| 2026-07-24 | PROPOSED: ordinary plugin hooks are conditional deterministic guardrails, not an absolute Tier-4 boundary | They are non-managed, definition-trusted, user-disableable, task-freshness-dependent, and incomplete for hosted/specialized paths | Preserve “cannot be reasoned around”; remove hooks entirely |
| 2026-07-24 | PROPOSED: a minimal verifier and exact runtime-bundle digest live literally in the trusted hook command; verified bytes execute from memory | The spike changed handler source without changing trust; a mutable bootstrap cannot authenticate itself; and verifying a path before importing it leaves a file-swap race. The definition-bound verifier must read once, hash, pin imports, and execute the same bytes | Mutable bootstrap self-check; plugin version; arbitrary revision token; verify-then-import; host definition hash without transitive verification |
| 2026-07-24 | PROPOSED: the existing `codex-enforcement-bridge` remains the legacy scaffold capability; plugin enforcement is a new additive capability | Its living Purpose intentionally describes a fail-open project dispatcher that depends on the Claude plugin, which is incompatible with the new plugin-owned adapter | Mutate and rename the legacy requirement in place; silently leave the old Purpose contradictory |
| 2026-07-24 | PROPOSED: the independent verifier is a separate fixed `codex exec --ephemeral -s read-only` process | A custom read-only agent inherited `danger-full-access` from its parent and wrote successfully; the separate read-only process was denied | Nested custom verifier; same-agent self-verification |
| 2026-07-24 | PROPOSED: use project-scoped custom agents for non-mutating advisory roles, not as verifier or mutation boundaries | `.codex/agents/*.toml` was discovered and right-sized correctly, but a custom read-only agent inherited a wider live parent sandbox | Undocumented plugin-packaged agents; native nested mutation; personal-only agent installation |
| 2026-07-24 | PROPOSED: Claude is an optional peer, never a governance prerequisite | The reason for moving the pilot is to remain governed when Claude auth/usage is unavailable | Block all Drydock work without Claude; silently downgrade while claiming agreement |
| 2026-07-24 | PROPOSED: Claude adapter success requires exit 0, valid schema, and no error flag | The unauthenticated CLI returned parseable JSON with `subtype: success` while `is_error: true` and exit code 1 | Trust the subtype or parseability alone |
| 2026-07-24 | PROPOSED: one plugin install plus explicit per-repository initialization | Plugin capabilities are global, while specs, context, agents, and packet state are repository-owned | Two host plugins; zero repository setup |
| 2026-07-24 | PROPOSED: no MCP server in the MVP | Skills, hooks, local scripts, and native agents cover the first useful host port with less privileged surface | Expose the lifecycle and orchestrator through a new MCP server immediately |
| 2026-07-24 | PROPOSED: implementation is additive and preserves the Claude plugin | This bounds rollback and keeps the proven product usable while Codex reaches parity | Immediate shared-directory refactor; replace Claude packaging in place |
| 2026-07-24 | SUPERSEDED: every mutating nested agent gets one dedicated worktree and reserved task branch | The worktree contract was necessary but not enforceable by a nested agent whose live parent can widen its permissions | Write in the Owner checkout; share one worktree among writers; rely on prompts to avoid merging |
| 2026-07-24 | PROPOSED: reuse only independently tested pure policy primitives, not Claude hook dispatchers or stateful entry points | `protect_secrets.check()` allowed `.env` for canonical `apply_patch` and unknown tools; packet/orientation/completion hooks maintain host-specific state | Reuse all hook modules as-is; duplicate policy logic without shared vectors |
| 2026-07-24 | PROPOSED: narrow matchers guard supported side-effect contracts; a mismatch inside a matched hook fails closed while unmatched tools remain uncovered | A broad fail-closed matcher could brick unrelated/read-only calls after a tool rename, while a narrow matcher preserves fail-closed behavior at the actual guarded boundary | Guess unknown payloads; broad catch-all deny; report a matched mismatch while allowing it |
| 2026-07-24 | PROPOSED: separate process establishes isolation, not epistemic diversity | Three Codex-family reviews used fresh read-only processes but shared a model family; Claude then found disjoint architectural blockers | Call a separate Codex process fully independent; treat process and epistemic boundaries as identical |
| 2026-07-24 | PROPOSED: authenticated Claude proof and a no-blocker cross-model re-review are Phase 0 gates | The reverse adapter is central to the requested workflow and only its unauthenticated failure path has been proven | Defer authentication until Phase 5; rely only on fakes |
| 2026-07-24 | PROPOSED: routing protects an Owner-configured coding-pace reserve rather than a hard spend ceiling | The Owner's north star is roughly three useful coding hours; flagship models coordinate and review while bounded workers type | Minimize spend unconditionally; use a fixed token budget; force judgment-heavy middle work to cheap agents |
| 2026-07-24 | PROPOSED: the host, OS, selected interpreter, hook definition, and trust store are an explicit trusted computing base | Codex definition hashes do not attest the interpreter binary; isolated/no-site startup and pinned imports prevent repository/plugin startup injection but cannot prove the whole host stack | Imply the inline digest is end-to-end binary attestation; ignore Python startup imports |
| 2026-07-25 | PROPOSED: mutating workers run as separate ephemeral Codex processes with fixed workspace-write roots, atomic worktree leases, and Owner-state fingerprints | A live probe proved the separate process could write inside its assigned worktree while the sandbox denied a sibling Owner-checkout write; nested permissions cannot provide this boundary | Nested writer plus post-hoc diff only; convention-only worktree assignment; reuse the parent sandbox |
| 2026-07-25 | PROPOSED: the runtime bundle is normalized plain UTF-8/LF Python and exact source-to-bundle rebuild comparison runs in CI and at release | Zip/pyz timestamps make naïve byte reproducibility unsatisfiable, and release-only comparison permits development-cycle drift | Zipapp without normalization; release-only rebuild; hand-maintained duplicate policy |
| 2026-07-25 | OWNER-APPROVED: Claude Opus 5 is an acceptable Phase 0 and runtime peer fallback when Fable is unavailable | The Owner explicitly accepted Opus 5 after the authenticated Fable call timed out; Anthropic exposes the fixed model ID `claude-opus-5` | Block all peer operation on Fable availability; silently use an unspecified moving alias |
| 2026-07-25 | PROPOSED: test peer transport/schema completion separately from substantive architecture convergence | A minimal two-turn Opus 5 probe completed successfully while the 202,820-byte high-effort review exceeded its 180-second process deadline; conflating them would misdiagnose transport or treat an instructed empty blocker list as peer agreement | Require every transport smoke test to perform a full review; count the transport probe as architectural convergence |
| 2026-07-25 | ACCEPTED PEER REVIEW: the corrected architecture converged with no blocking concerns | Claude independently confirmed that separate fixed-root mutation processes, narrow hook matchers, reproducible bundle CI, and host-native policy boundaries close its prior blockers; Phase 0 transport had already passed locally | Treat non-blocking gaps as blockers; begin implementation without Owner approval |
| 2026-07-25 | PROPOSED: mutation leases are reclaimable only after timeout-plus-grace and exact dead-process proof; the runner owns Git metadata mutations and orphaned-lease cleanup | A worktree's linked `.git` metadata can sit outside the worker sandbox, and a crashed worker can strand a lease; explicit ownership and bounded reclamation avoid denied Git writes, deadlock, and unsafe second writers | Let workers stage/commit through linked metadata; steal leases by age alone; never reclaim leases |
| 2026-07-25 | IMPLEMENTATION EVIDENCE: the Codex plugin root is `adapters/codex/drydock/`, with a deterministic bundled copy of the project scaffold | The current plugin validator requires `skills` to resolve to plugin-root `skills/` and rejects a manifest hook override; a repository-root Codex manifest would therefore auto-discover the existing Claude hooks and violate the additive boundary. A generated digest-bearing scaffold bundle preserves one-install initialization without hand-maintained third copies | Repository-root Codex manifest; mutate protected root skills/hooks in slice one; standalone plugin with no initialization assets; manually maintain a third scaffold tree |
| 2026-07-25 | IMPLEMENTATION EVIDENCE: the Claude peer exposes only `StructuredOutput`; default permission mode replaces plan mode | Claude Code implements `--json-schema` through its schema-return tool, so disabling every tool silently produced unstructured text on the installed 2.1.173 CLI. Plan mode also changed turn semantics. Safe mode, strict empty MCP configuration, Chrome disablement, and the one-tool allowlist provide the side-effect boundary; a supported-subset schema then completed live on exact Opus 5 | Empty tool list that disables the required output mechanism; plan mode as the authority boundary; enabling repository, shell, web, Chrome, or MCP tools |
| 2026-07-25 | IMPLEMENTATION CORRECTION: the child loads Owner-trusted Codex config but fixes and reports a reduced runtime surface | On Codex CLI 0.146.0-alpha.3.1, `--ignore-user-config` also removed repository trust and silently downgraded an explicit workspace-write child to read-only. Loading config restored the sandbox, while empty MCP, disabled web/network/root expansion, ignored rules, reduced environment, and disabled plugin/app/browser/computer-use features prevented the observed integration widening. The Owner config and host trust store are therefore explicit TCB, not hidden assumptions | Claim clean-config isolation; load user config without fixed overrides; fall back to danger-full-access |
| 2026-07-25 | IMPLEMENTATION CORRECTION: no-change mutation is BLOCKED, and verifier `-C` is context rather than read confinement | Dogfood produced a zero-exit worker that said writes were denied and changed nothing; the initial runner falsely returned green N/A. A regression now returns `no_changes`. A separate verifier preserved the tree but read user plugin metadata outside its root before timing out, so only write isolation is proven | Treat no diff as success; call read-only mode root-confined; convert verifier timeout into PASS |
| 2026-07-25 | ACCEPTED PEER BLOCKER: post-worker Git is pinned away from the writable `.git` link and uses temporary snapshot metadata | Opus 5 showed that following a worker-controlled linked-worktree pointer could redirect the trusted runner into Owner Git metadata. The corrected runner captures the administrative directory from trusted main-repository metadata, validates the link, passes explicit Git roots, strips ambient `GIT_*`, and stages only into a temporary index/object directory | Trust Git discovery in the worker root; stage into the linked or Owner index; document the redirect as residual risk |
| 2026-07-25 | ACCEPTED PEER BLOCKER: the complete worker process tree is quiesced before evidence capture | A direct child can exit while a background descendant continues mutating. Windows workers now start suspended inside a kill-on-close Job Object; POSIX workers use a separate process group; the boundary is terminated before lease release, fingerprinting, or diff extraction | Check only the direct PID; snapshot while descendants may be live; detect post-snapshot drift later |
| 2026-07-25 | ACCEPTED VERIFIER BLOCKERS: nested verdict/state validation and finite peer economics are fail-closed | The separate Codex verifier demonstrated that shallow top-level checks accepted schema lookalikes and Python JSON accepted `NaN`, whose comparisons bypassed a cost ceiling. Verifier output now echoes schema-bound state and is deeply validated; non-finite JSON, budgets, and costs are rejected | Rely only on CLI schema enforcement; compare arbitrary Python floats; treat parseability as validity |
| 2026-07-25 | ACCEPTED PEER BLOCKERS: verifier quiescence, Windows junction refusal, and applicability default were corrected | Opus confirmed the mutation Git/job fixes, then found that the verifier had not closed its Job Object before evidence capture, no-privilege Windows junctions were untested, and an extension allowlist made unknown build/config paths test-N/A and green. The verifier now quiesces first; pre-extract/pre-cleanup scans reject reparse paths; unknown paths default to verification-required; test-N/A remains non-green | Treat read-only descendants as harmless; count privileged-symlink skips as junction coverage; enumerate every risky suffix |
| 2026-07-25 | IMPLEMENTATION LIMIT: descendant-lifetime quiescence is proven only for the Windows Job-Object path and is not a filesystem-confinement claim | A POSIX process-group descendant can call `setsid()` and escape group termination, while a Windows Job Object proves process lifetime shutdown but does not restrict writable paths. The runner reports the exact mechanism and relies on the separately tested fixed-root sandbox for filesystem confinement | Claim a Job Object confines filesystem writes; claim process groups contain hostile descendants; remove POSIX support without reporting why |
| 2026-07-25 | IMPLEMENTATION HARDENING: worker Git attributes, non-finite leases, and ambiguous cleanup paths fail closed | Worker-authored attributes can suppress diff content; non-finite lease deadlines can skip or wedge reclamation; Windows path aliases can strand leases. Attribute changes now invalidate review, diff helpers are disabled, lease JSON/timing is strict and finite, and lease paths are canonicalized | Defer adjacent findings; rely on operator interpretation of partial diffs and malformed leases |
| 2026-07-25 | ACCEPTED PEER BLOCKER: direct Git-control fingerprinting precedes every post-worker Git command | Opus showed that Job-Object lifetime proof does not prevent a sandbox escape from poisoning repository-local `core.fsmonitor` or `core.hooksPath`, which trusted fingerprint/extraction Git could then execute. The runner now captures a bounded direct-byte control fingerprint before launch, rechecks it after quiescence before Owner fingerprinting or extraction, pins executable local Git config, rechecks after extraction, and has a hostile sentinel regression | Treat lifetime containment as filesystem containment; invoke Git to discover whether Git config was poisoned; document executable local config as residual risk |
| 2026-07-25 | IMPLEMENTATION HARDENING: mutation is a handoff state, not a green terminal state | The earlier `ok: true` branch required a gate verdict the applicability function never emitted. Mutation now always returns non-green stages (`awaiting_verification`, `review_required`, `ignored_artifacts`, or a failure) until separate verification and deliberate integration; strict JSON also rejects duplicate keys, and Windows process identity is captured while suspended | Preserve unreachable success code; let last duplicate JSON key win; identify the process only after it can exit |
| 2026-07-25 | FINAL SELF-REVIEW HARDENING: pin the Git binary and refuse config-driven external filters/includes | A bare `git` invocation from the writable worktree could enter Windows executable search, while `git add` can execute configured clean/process filters even with hooks and fsmonitor disabled. The runner now resolves Git only from absolute PATH entries before delegation, invokes that absolute file for every Git operation, and refuses local include/filter configuration before worktree creation | Rely on PATH lookup from an untrusted cwd; treat disabling hooks as disabling filters; add executable names to an ever-growing worktree denylist |
| 2026-07-25 | FINAL SELF-REVIEW HARDENING: pin the preflight Git call and bound worktree traversal | The reserved-container ignore preflight was the last runner-owned Git call using executable search, and the no-follow post-worker scan could traverse an attacker-inflated tree without a finite limit. The preflight now uses the same absolute Git discovery/config pinning as the runner, and boundary scans stream entries and refuse after 100,000 before extraction or cleanup instead of first materializing a directory | Narrow the absolute-Git claim to post-creation calls; rely on worker timeout to bound later traversal; report a partially inspected tree as evidence |
| 2026-07-25 | OWNER-APPROVED: remediate the bounded LaunchGuardian/CI release-gate slice | After the runner peer gate converged, the Owner replied “Cool lets go” to the stated next step: resolve LaunchGuardian dispositions and CI action pinning, rerun the strict gate, then return to final Codex-host verification | Leave known mutable CI tags; infer approval for scanner suppression or release |
| 2026-07-25 | PIN PLAN: preserve the workflow's current major behavior at the latest signed v4/v5 releases and replace mutable tags with full SHAs | Official GitHub release pages and direct official-repository refs agree on `actions/checkout` v4.4.0 at `11d5960a326750d5838078e36cf38b85af677262` and `actions/setup-python` v5.6.0 at `a26af69be951a213d495a4c3e4e4022e16d87065`. Pinning the exact commits removes tag-repointing risk without a major upgrade | Keep mutable major tags; upgrade to v5/v6 in the same security patch; trust a third-party SHA list |
| 2026-07-25 | DOC PLAN: retain the historical experimental local WebSocket transport claim without the scanner-triggering plain-scheme literal | The finding is in Markdown prose and no executable socket exists. Rephrasing the sentence is a semantic-preserving documentation correction, not evidence that a shipped insecure service was fixed | Add a Semgrep ignore; delete the capability finding; relabel it as production TLS |
| 2026-07-25 | RELEASE-GATE EVIDENCE: immutable action pins and the documentation false positive are resolved; six supported-runtime policy mismatches remain blocking | The strict scan at `2026-07-25T14:03:54.497175Z` ran all five adapters with valid LGF configuration and reported 6 high, 0 medium. The only findings are Semgrep Python-3.6 compatibility rules for `Popen` arguments in a Python-3.9+ project. LaunchGuardian 0.2.0 has no reviewed per-finding disposition or supported-runtime profile, so the scan remains blocked without suppression or waiver | Rewrite deterministic UTF-8 subprocess handling for an unsupported runtime; add inline ignores without approval; describe scanner execution as rule-completion proof despite 20 raw fixpoint-timeout warnings |
| 2026-07-25 | OWNER-APPROVED: record two exact compatibility-rule dispositions as `not_applicable` | After receiving the exact proposal—two named Semgrep Python-3.6 `Popen` rule IDs, the Python-3.9+ project floor, CI/readiness evidence, Owner attribution, and an `APPROVED_WITH_DISPOSITIONS` outcome—the Owner replied “lets keep going then.” Approval is limited to those two rule IDs and does not authorize wildcard suppression, Critical downgrades, release, or publication | Infer a general scanner waiver; hide raw findings; treat approver text as cryptographic identity proof |
| 2026-07-25 | INTEGRATION PLAN: exercise the reviewed LaunchGuardian source build before any PyPI release | Installed LaunchGuardian 0.2.0 cannot parse reviewed dispositions. Companion commit `24abba5c9cd3eb723356e7ec0de640b7c8278680` adds exact matching, fail-closed validation, visible disposition evidence, schema 0.2.0, and pinned UTF-8 scanner subprocesses; its 80-test suite, package build, and strict self-scan pass. Drydock verification invokes that source checkout explicitly so a local result is not mislabeled as evidence about the still-installed PyPI package | Modify the installed package in place; publish before Drydock proves the behavior; call a source-only pass proof that PyPI 0.2.0 supports it |
| 2026-07-25 | RELEASE-GATE EVIDENCE: reviewed source build returns `APPROVED_WITH_DISPOSITIONS` while retaining every finding | The strict scan from companion commit `24abba5c9cd3eb723356e7ec0de640b7c8278680` at `2026-07-25T15:16:29.577002Z` ran all five scanners with valid LGF configuration. Raw Semgrep contains exactly 6 results from the two approved rule IDs and 0 errors. The normalized report retains all 6 as High with original `blocks_launch: true`, adds the Owner-reviewed disposition, sets status `not_applicable`, and reports 0 open blockers. Installed/PyPI 0.2.0 remains unchanged | Delete findings from output; change severity or underlying blocking metadata; report plain `APPROVED`; imply approver metadata authenticates identity |
| 2026-07-25 | COMPANION RELEASE HARDENING: pin every repository and distributed-template Action to a verified full commit SHA | LaunchGuardian descendant commit `c754062dc1c35dce06cfc6f7946909287f1ce1fc` pins all 16 Action references while retaining readable release comments and adds a regression that rejects mutable refs. Its suite returns 81 passed. A strict self-scan at `2026-07-25T15:27:56.088651Z`, after moving generated `build/` residue outside the source tree, ran all five scanners and returned point-in-time `APPROVED` with 0 normalized findings and 0 raw Semgrep errors. This does not alter or replace the separate Drydock disposition evidence from commit `24abba5c9cd3eb723356e7ec0de640b7c8278680` | Treat an ignored generated package copy as source; add a scanner exclusion for build residue; call one clean self-scan universal proof; publish the descendant without final peer review |
| 2026-07-25 | FINAL PEER GATE: the exact two-repository Opus review converged with no blocker | The Owner-relayed read-only review verified both Git identities, reproduced every reported deterministic count, decoded and audited the hook bootstrap, exercised integrity denials, inspected runner/verifier/peer failure boundaries, verified disposition arithmetic and retained findings, and returned `converged: true`. Five gaps, six risks, and four non-blocking follow-ups remain recorded; notably, personal install/trust/current-revision liveness is still unproven and Action SHA authenticity was not network-reproduced by that reviewer | Treat earlier timeouts as convergence; erase non-blocking findings; call same-family review epistemic independence; mark the dogfood or release gate complete |
| 2026-07-27 | DOGFOOD BLOCKER: trusted Desktop SessionStart runs, but the readiness skill does not bind its record | The Owner reviewed and trusted all Drydock hooks through the CLI, then started a fresh Desktop task. SessionStart wrote task `019fa333-1ff4-76d0-901d-2d392973f80b` with the exact runtime digest and repository root, but the documented readiness invocation omitted `--session-id` and `--plugin-data` and returned `unavailable`. Passing those exact values returned `current_revision_observed`. The hook mechanism worked; the readiness adapter failed to connect the evidence | Ask the Owner to repeat trust/new-task loops; call file presence liveness; scan arbitrary historical records; mark dogfood complete from the manually repaired command |
| 2026-07-27 | DOGFOOD CORRECTION: bind only the current host task to one exact plugin-data record | Readiness now prefers explicit diagnostic arguments, otherwise accepts a validated `CODEX_THREAD_ID`, scans at most 256 direct children of the current Codex plugin-data directory without following directory symlinks, requires exactly one regular liveness record, and validates task ID, runtime digest, and resolved repository root. Resolution sources and failures are reported. `CODEX_THREAD_ID` is observed behavior on the tested build, not claimed as a stable public contract | Select the newest historical record; recurse without a bound; accept multiple plugin roots; infer full hook trust/coverage or `ready_for_enforcement` from SessionStart liveness |
| 2026-07-27 | DOGFOOD EVIDENCE: corrected installed readiness binds the fresh Desktop task without overrides | The refreshed local cache matched all 15 reviewed source files byte-for-byte. In task `019fa333-1ff4-76d0-901d-2d392973f80b`, the ordinary skill command returned `current_revision_observed`, `session_id_source: CODEX_THREAD_ID`, `plugin_data_source: codex_home_plugin_data`, exact repository root, model, permission mode, and runtime digest. It retained `trusted: unknown`, empty covered paths, `active: false`, and `ready_for_enforcement: false` instead of promoting SessionStart evidence into a full enforcement claim | Call SessionStart liveness proof that both PreToolUse matchers intercepted this task; hide bypass-permission context; mark managed or Tier 4 |
| 2026-07-27 | DOGFOOD EVIDENCE: both installed narrow PreToolUse contracts deny inert secret-path probes in the live Desktop task | In the same task with `current_revision_observed`, canonical `Bash` denied `Set-Content -WhatIf` against `.env.drydock-live-probe-7f1c9e42`, and canonical `apply_patch` denied an update of that guaranteed-absent path. Both returned the exact Drydock secrets-guardrail reason. A final `Test-Path` returned `False`. The probes were non-mutating even if interception failed: `-WhatIf` suppresses the shell write and Update File cannot create an absent file | Use a destructive Git probe; infer coverage for MCP, hosted, specialized, or other local tools; promote two point-in-time matcher observations into managed, universal, or Tier 4 enforcement |
| 2026-07-27 | SCOPED PEER GATE REOPENED: thread-keyed liveness replays across resumes | The exact-Opus-5 review of `07d2be3..40adbf5` returned `converged: false` with one blocker. The record has no timestamp, nonce, or per-invocation marker; the positive regression itself proves a hand-written matching record returns `current_revision_observed`. Disabling hooks and resuming the same thread can therefore replay stale evidence. The first wrapper lost its output pipe and was not counted; one clean retry completed in 201.3 seconds at `$0.6880055` and supplied the verdict | Treat task ID/digest/root equality as freshness; call a process exit a verdict; weaken only prose while retaining the misleading status; proceed to integrated dogfood before correction and re-review |
| 2026-07-27 | SUPERSEDED PROPOSAL: compare the Bash PreToolUse marker with a cross-process monotonic clock | The focused hook/readiness regression disproved the premise on this machine: the hook's `py -3` selected Python 3.14 while readiness used Python 3.11, and their monotonic readings differed by about 43.7 seconds even though their wall clocks differed by about 67 milliseconds. The test remained failed; no future-dated marker was accepted | Widen the monotonic window until the test passes; treat Python's per-interpreter monotonic epochs as comparable by assumption |
| 2026-07-27 | PROPOSED CORRECTION: readiness requires an explicit CLI hook probe and a recent Bash PreToolUse marker inside bounded system-clock age and future-skew windows | SessionStart must preserve its packet baseline, so only a supported Bash command containing the readiness subcommand and `--hook-liveness-probe` writes a separate atomic activity record. The readiness process must also confirm the probe flag was requested. The OS wall clock is comparable across the tested Python 3.11/3.14 interpreters and is already inside the explicit host/OS TCB. A 10-second maximum age plus 2-second future-skew allowance rejects stale resumes and material clock jumps; an in-window replay remains disclosed. The record remains unsigned and user-writable, so the report retains non-managed/inactive enforcement fields | Write a marker for every Bash call and call it same-invocation proof; overwrite the SessionStart packet baseline; accept an unbounded wall-clock timestamp; claim cryptographic provenance |
| 2026-07-27 | SCOPED PEER CONVERGENCE: explicit probe plus bounded recent activity closes the resume-replay blocker for live dogfood | Exact Opus 5 reviewed `40adbf5..79cb83f` in 218.5 seconds and returned `converged: true` with no blocker. It accepted the 10-second/2-second bounds and unsigned/in-window replay disclosure for the cooperative non-managed profile, while explicitly withholding release/archive/merge/publication authorization. Its precision gaps were then fixed before the install candidate: fresh activity now owns model/permission fields; probe tokens require `drydock_codex.py readiness` plus an exact flag; markers write only after deterministic allow; payload event, apply_patch non-marking, denied-probe non-marking, activity junction, time boundaries, and the decoded digest chain are tested. Code inspection confirms liveness is evaluated before the optional peer status subprocess | Promote the result to release readiness; skip exact-digest live dogfood; cite unbounded SessionStart model/permission context as current; leave loose documentation-string probe matching |
| 2026-07-27 | PLATFORM DOGFOOD FINDING: a visible Desktop close did not reload refreshed hook definitions because the old backend survived | Independent `hooks/list` inspection showed the current definitions enabled, trusted, and hash-matched, but new tasks created by the still-running `codex.exe` and `codex-code-mode-host.exe` backend did not dispatch them. After saving work and fully terminating those processes, the restarted Desktop emitted SessionStart orientation and PreToolUse activity. Operator guidance now requires a full backend restart after refresh or trust changes before fresh-task proof | Repeat trust toggles; infer hook failure from stale-process tasks; treat a checked `/hooks` entry as current-task liveness |
| 2026-07-27 | CURRENT-REVISION DOGFOOD EVIDENCE: exact handler liveness and both narrow deny contracts were observed without promoting enforcement | The installed cache matched all 22 source files at commit `d4d63111b4d82735cd135cb8439a3c97bd26d568`. Fresh task `019fa3ba-1935-7e73-87da-6587d087ea2a` reported `current_revision_observed` for handler `a04cf380e435e4a39640d10886fcf82d0176233ee6b974854bb03cc158c9bc4b` from a 363-millisecond-old Bash PreToolUse record with exact root/digest and fresh model/permission context. Separate inert live probes showed canonical Bash and apply_patch both denying `.env.drydock-a04c-live-probe`, and `Test-Path` returned `False`. Readiness retained unsigned/replay caveats, `active: false`, `trusted: unknown`, no covered paths, and `ready_for_enforcement: false` | Infer unsupported path coverage; call UI trust host attestation; promote cooperative point-in-time evidence into managed, universal, or Tier 4 enforcement; authorize release |
| 2026-07-27 | DOGFOOD GAP: one large multi-file apply_patch failed closed with a generic integrity message although the runtime bytes matched | The denied invocation made no changes. Immediate direct SHA-256 measurements returned the expected `a04cf380...` digest for installed and source `runtime.py`, their text compared equal, and later smaller guarded patches succeeded. This establishes a conservative false denial or transient bootstrap failure, not its cause and not a detected tamper | Hide the denial; call the hash check proof that the bootstrap could not have failed; bypass the hook; turn an unresolved availability/diagnostic issue into an enforcement pass |
| 2026-07-27 | PEER TRANSPORT FAILURE: two final exact-handler Opus CLI attempts returned no verdict and do not count as review | The first review bundle duplicated current sources after the full delta and returned `error_max_budget_usd` after 222.2 seconds at a configured `$1.25` ceiling. One right-sized retry removed the duplication but returned the same failure after 283 seconds. Neither result contained a schema-valid envelope, observed-model usage, critique, or cost evidence. Automated retries stopped; the existing Claude chat receives a bounded read-only relay instead | Infer convergence from authenticated process execution; claim exact model observation from the requested model field; keep raising budget without Owner direction; call a transport failure a blocker in the implementation |
| 2026-07-27 | FINAL EXACT-HANDLER PEER CONVERGENCE: the Owner-relayed repository-aware Claude review returned no blocker | The review verified the bounded Git identities, reproduced focused 37/1, adapter 109/2, sync 11/11, packet 38/2, and decoded all eight generated hook variants to the current a04cf380 digest chain. It found no false green or unsupported enforcement claim. The manual relay lacks a successful controller envelope, so exact model observation is not claimed from controller evidence | Count either failed CLI call as review; erase the manual-route limitation; promote peer convergence to release, archive, merge, publication, or deployment authority |
| 2026-07-27 | PEER-DIAGNOSED GAP: isolated Python inherits locale stdin and mislabels decode failure as integrity failure | The peer reproduced ASCII allow, silent em-dash mis-decoding, and a C2 81 byte denial with runtime bytes unchanged. Under `-I -S`, environment encoding overrides are ignored; `sys.stdin.read()` can fail under cp1252 and the broad bootstrap exception borrows the integrity message. This is fail-closed and non-blocking for the scoped MVP, but UTF-8 byte decoding, failure-taxonomy separation, a byte-level regression, stricter executed-program probe recognition, and Windows tokenization remain tracked follow-ups | Call the denial detected tamper; ignore altered non-ASCII text; fix it inside the reviewed digest before integrated dogfood and silently invalidate current-revision evidence; treat non-blocking follow-ups as waived |
| 2026-07-27 | INTEGRATED DOGFOOD NEGOTIATION: stop at the two-round cap without inferred agreement | Exact Opus 5 returned non-converged schema-valid critiques in both rounds. Codex accepted the Round-1 evidence, wording, target, baseline, and re-verification corrections. The final concerns were a worktree-Git redirect chain that current direct-pointer checks, explicit Git roots, temporary-index extraction, ignored-artifact enumeration, and a hostile regression already address, plus a requested UTF-8 discriminator that Codex then reproduced: U+0081/U+008D deny only in locale mode and allow with `-X utf8=1`, with the runtime digest unchanged. The peer has not seen this rebuttal evidence. No mutating worker started | Exceed the cap silently; treat reviewer assertions as true without code audit; infer agreement from Codex's rebuttal; start mutation before Owner direction |
| 2026-07-29 | REBASE THE UNRUN DOGFOOD PLAN BEFORE MUTATION | The frozen guide preimage advanced by 99 reviewed orchestration-efficiency lines after the unchanged insertion anchor, and the hardened verifier now requires a clean candidate plus an accepted exact-fingerprint `full_required_suite` proof record. The note is still absent and its bytes and insertion offset are unchanged. The plan now registers the current guide blob/postimage, commits the reviewed diff on the isolated branch before proof, runs one exact-commit full suite, and supplies the required packet root and proof record to both separate verifier calls. Ordinary ignored bytecode caches remain disclosed but are not falsely required absent; only current blocked injection paths are gating. A fresh peer critique is required before any worker starts | Run the stale hash/API contract; waive the first verifier; treat the previous peer result as approval of a materially changed proof flow; create a partial living spec to avoid the dependency |
| 2026-07-29 | REBASED DOGFOOD ROUND 1: keep the worker blocked and reconcile four peer blockers by mechanism | Exact Opus 5 returned a schema-valid non-converged critique against clean HEAD `347d06a...` and v2 fingerprint `c1ae2c92...`. The preimage-freshness and egress-wording blockers are accepted. Source audit refuted only the claim that the runner leaves hooks outside every fingerprint: it already recursively fingerprints common hooks and pins null hooks/fsmonitor/external diff/textconv with scrubbed Git config. Equivalent explicit pins and broader local URL/credential/config/remote checks are now required for the later control-plane commit/push. Round 2 receives the complete frozen note and context. No worker, worktree, integration, or push started | Treat four blockers as automatic truth without source audit; ignore them because the old plan once converged; change the already-reviewed runner unnecessarily; start the worker before Round 2 |
| 2026-07-29 | REBASED DOGFOOD ROUND 2: budget ceiling is no verdict; return to Owner | The only remaining `$0.50` inside the two-call `$2.00` phase envelope was insufficient for the focused Round-2 request. After 205.04 seconds the Claude CLI returned exit 1 with `error_max_budget_usd`; no critique envelope or schema verdict existed. The controller classified `structured_budget_ceiling`, kept `gate_satisfied: false`, required `return_to_owner`, and the durable run was closed `blocked`. No third call, worker, worktree, integration, or dogfood push occurred | Infer closure from elapsed/cost/model authentication; silently start a fresh run to reset the envelope; call the transport ceiling a peer blocker; weaken or skip pre-mutation critique |
| 2026-07-29 | OWNER AUTHORIZED ONE FRESH SUPERSEDING DOGFOOD CYCLE AND CONDITIONAL PUSH | The authorization is for the existing `codex-host-mvp` objective only: create a new durable run with closed run `73318eaf444b429d952e0fbe09119056` recorded as the previous run, at most two peer calls at `$1.50` each and `$3.00` total peer-phase ceiling. A single non-force push to `origin/codex/codex-host-mvp-checkpoint` is allowed only after plan convergence, isolated mutation, cross-review, exact proof, separate verification, integration, clean-tree, preimage/remote, and every packet gate pass. Any unavailable, failed, stale, malformed, mismatched, unknown, or unproven gate stops. Force, another destination, archive, release, publication, deployment, weakened verification, and a third peer round remain unauthorized | Resume or reinterpret the closed run; silently reset envelopes; treat approval as a gate override; push before the complete chain passes |
| 2026-07-29 | CLOSED RUN LINEAGE STAYS IN PACKET EVIDENCE, NOT `previous_run_id` | `start-run --previous-run-id 73318eaf...` failed before creating a run because the controller permits only an active run to be superseded. Reopening or rewriting the blocked ledger would contradict the Owner's instruction. The authorized cycle therefore creates an independent new durable run; this packet records the exact predecessor and failed start result without calling the old run resumed, restarted, or superseded in-place | Change the closed ledger to active; omit lineage; reuse the old run ID; treat the failed start as a new run |
| 2026-07-29 | SUPERSEDING DOGFOOD ROUND 1: close four irreversible-tail blockers before the final peer call | Run `7156538ebf4a4f0ea4ae283a3bf734bb` returned a schema-valid non-converged exact Opus 5 critique at `$0.949309` provider-reported cost against clean HEAD `173ea1b...` and v2 fingerprint `f781a632...`. The plan now binds push authority to the verbatim current-task Owner turn and exact destination, replaces Git config denials with an exact allow-list plus fail-closed SSH/host-key evidence, runs verifier #2 on a second fresh exact-commit materialization before moving the Owner branch, and separates Codex byte binding from peer semantic review. The frozen note and splice bytes are unchanged. One plan-peer call remains; no worker, worktree, integration, or push started | Infer convergence from strong positive commentary; leave the Owner branch advanced on a verifier-2 failure; ask the peer to attest bytes it cannot observe; use ambient SSH redirects or interactive host-key acceptance |
| 2026-07-29 | SUPERSEDING DOGFOOD ROUND 2: cumulative input ceiling blocks the final peer call | The revised 38,011-byte Round-2 request plus Round 1's 28,159 bytes would total 66,170, exceeding the phase ceiling by 634 bytes. The controller refused before provider spawn with `envelope_exhausted`, `critique_skipped: true`, `gate_satisfied: false`, and `return_to_owner`. The durable ledger therefore remains at one provider call and `$0.949309` observed provider cost and was closed `blocked`. The controller suggested a smaller scope, but the Owner's one-use authorization requires stop after a failed/unproven gate; no retry, worker, worktree, verifier, integration, or push occurred | Treat a pre-spawn refusal as a peer verdict; silently shrink and retry after the controller returned blocked; infer that the four revised blockers converged because the plan changed; consume the conditional push authority |
