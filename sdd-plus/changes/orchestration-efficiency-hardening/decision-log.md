# Decision Log

## Change

orchestration-efficiency-hardening

## Decisions

| Date | Decision | Reason | Alternatives Considered |
| --- | --- | --- | --- |
| 2026-07-28 | Efficiency work may remove duplicate work but not peer critique, independent verification, fail-closed gates, or final evidence | The Owner explicitly wants Drydock faster without making it less safe | Reduce proof or allow implementers to self-certify |
| 2026-07-28 | Treat the adaptive-ledger run as dogfood evidence, not a universal calibration dataset | One FULL run exposes mechanisms and failure modes but cannot justify precise global thresholds | Invent exact token savings and universal time limits |
| 2026-07-28 | Separate the first implementable hardening slice from a future repository-aware peer transport | Budget classification, single-flight, durable result capture, input preflight, and proof invalidation are bounded; safe repository read access needs its own threat model | Give the existing peer unrestricted Read tools immediately |
| 2026-07-28 | Key reusable proof to candidate, exact command, and relevant environment; unknown relationships invalidate | Reuse is safe only when the evidence still describes the same executable state and environment | Cache by branch name or latest successful result |
| 2026-07-28 | Peer budget violations return to the Owner | The active peer-unavailable delta already requires this, and live Opus/Fable calls exposed that the implementation currently continues automatically | Treat max-budget as ordinary availability failure |
| 2026-07-28 | Do not calibrate a peer ceiling from input bytes alone | A 15,824-byte pre-implementation Fable request still exceeded a $0.50 ceiling after 115 seconds; reasoning and output behavior materially affect cost | Assume every small plan fits a fixed cheap ceiling; raise the ceiling automatically |
| 2026-07-28 | Invert peer failure handling to a benign-availability allowlist | The current catch-all maps every unknown subtype to process_failure and automatic single-pilot continuation; fixing only error_max_budget_usd would preserve the fail-open class | Add one named subtype to the existing classifier |
| 2026-07-28 | Trigger pre-mutation critique from objective change properties | A packet must not opt out merely by omitting peer convergence from its own declared gates | Let the packet author decide whether critique applies |
| 2026-07-28 | A cheaper post-exhaustion model is advisory only | Model identity is load-bearing for convergence and verification; disclosed downgrade still weakens the gate | Let any successful fallback satisfy the original gate |
| 2026-07-28 | Require a full suite against the final executable-surface fingerprint | Composed proof accelerates intermediate work but is not final acceptance evidence; exact evidence-only records are tracked separately to avoid self-invalidating regress | Compose passing commands from different executable states; include the result record in the digest it must attest |
| 2026-07-28 | Disable proof reuse on dirty or untracked trees | A complete clean Git tree binds tracked loadable files; untracked conftest/site hooks and unknown environment relationships otherwise escape the fingerprint | Hash only the changed diff |
| 2026-07-28 | Never restart an expired peer lease automatically | A dead local process does not prove the remote paid call stopped; automatic restart can duplicate spend | Permit one implicit restart |
| 2026-07-28 | Persist only screened bounded terminal results outside the repository for at most 24 hours | Recovery needs a short durable window without making peer output an accidental-commit or long-lived content archive | Store results in the worktree indefinitely; store only a digest and lose recovery |
| 2026-07-28 | Partition executable-surface proof from packet-evidence records | A full-suite result cannot be committed into a fingerprint that includes itself without infinite regress; an exact evidence-only allowlist terminates while any behavior-affecting byte still invalidates | Hash the entire commit including the result it must record; exclude all packet files broadly |
| 2026-07-28 | Retarget failure classification to `Claude is an optional, bounded, structured peer` and explicitly supersede its broader predecessor wording | Delta sync matches requirement names; a novel MODIFIED heading would leave the old permission alive beside it | Add a second differently named requirement and rely on implementers to choose the stricter one |
| 2026-07-28 | One run spans all invocations for one Owner objective; no cross-run weekly ceiling is claimed | Per-process resets would not bound the observed multi-turn harm, while the current controller lacks trustworthy cross-run account accounting | Define run as one subprocess; imply weekly protection |
| 2026-07-28 | Logical result expiry is enforced on every read/start and stale bodies are deleted then | Drydock cannot physically delete while it is not running, so an unconditional wall-clock deletion claim lacks an actor | Depend on an always-running sweeper |
| 2026-07-28 | Bare nonzero peer exits may continue only when provider cost is proven zero | Unstructured policy/budget aborts are otherwise indistinguishable from crashes; unknown or positive spend is not proven benign | Treat every unstructured nonzero exit as ordinary availability failure |
| 2026-07-28 | Separate ignored code-injection paths from derived bytecode caches | Ignored conftest/site hooks can change behavior and must block reuse, while ordinary test execution creates bytecode caches and would otherwise make reuse impossible; proof instead runs from a fresh bytecode-free commit root with bytecode writes disabled | Disable reuse whenever any ignored bytecode exists; silently purge unexplained files and continue |
| 2026-07-28 | Disclose a packet-evidence-parent fingerprint, never a self-inclusive report digest | `verification.md` cannot contain the hash of an evidence set that already includes those same bytes | Claim an impossible fixed-point digest; omit the evidence-set identity entirely |
| 2026-07-28 | Define zero provider cost only from a parsed requested-model CLI envelope with finite `total_cost_usd == 0` | A bare nonzero exit otherwise gives no trustworthy basis for treating spend as absent; missing or malformed cost must remain unknown | Infer zero cost from process timing or lack of contract output |
| 2026-07-28 | Validate allowlisted review JSON against a versioned packet-local schema | Evidence-only status is safe only for narrow, structurally constrained records | Exempt any JSON at the packet root |
| 2026-07-28 | Require an explicit recorded Owner action to reset the durable run | A controller-declared new objective after exhaustion would defeat the cumulative ceiling | Let model output, a worker result, or exhaustion create a fresh run ID |
| 2026-07-28 | Stop using the isolated Codex mutation runner for the remaining implementation after one inert broad attempt and one extreme-overhead focused attempt | The broad worker ran eight minutes with zero writes; the focused three-file worker consumed 914,677 input tokens (806,144 cached) and created ignored test caches before returning its diff. Continuing that path would reproduce the efficiency failure this packet exists to fix | Spend additional weekly capacity on more isolated workers; weaken the final review or verifier gates |
| 2026-07-28 | Treat direct pilot implementation after that stop as disclosed dogfood, not containment evidence or a new default | The Owner asked to finish while preserving safety; exact scoped diffs, focused tests, peer cross-review, a frozen full-suite pass, and separate verification remain required, but this implementation slice does not claim fixed-root mutation containment | Pretend the runner completed the whole implementation; report direct edits as equivalent isolation |
| 2026-07-28 | Replace working-tree v1 proof identity with versioned, domain-separated v2 identity over exact committed Git paths, modes, types, and blob bytes | Git can report a Windows checkout clean while newline-normalized checkout bytes and platform modes differ from the committed bytes materialized by proof-run | Continue hashing checkout bytes; normalize the checkout in place |
| 2026-07-28 | Dual-project only the exact target packet `tasks.md`: normalized canonical state enters executable identity and raw bytes enter lifecycle evidence | Task wording and ordering affect governance behavior, while final status transitions must not invalidate the proof they record | Exclude all tasks from executable proof; normalize every packet or template |
| 2026-07-28 | Treat a noncanonical task file as fully executable with an explicit projection-declined diagnostic | BOM, CR, encoding, or grammar divergence must remain conservative without making fingerprint computation unavailable | Abort all fingerprinting; silently normalize uncertain syntax |
| 2026-07-28 | Make lifecycle evidence identifying but non-attesting and non-self-gating | Stop/archive state can change after verifier PASS without changing executable bytes; the verifier attests only the executable candidate | Claim unchanged executable identity means unchanged governance state |
| 2026-07-28 | Require proof archives to match committed tree paths, regular-file bytes, and executable-mode semantics | Future `export-ignore`, `export-subst`, gitlinks, or tracked links must not recreate a digest/proof-root mismatch | Assume `git archive` always materializes the raw tree |
| 2026-07-28 | Serialize stale ledger-lock recovery with an OS advisory lock and a 60-second persisted lease | Removing a stale path after a separate read permits competing recoverers to replace each other's live lock; holding the file lock makes crash recovery and live-holder refusal one serialized transition | Permanently fail closed on every existing lock; unlink a stale lock and retry without serializing recovery |
| 2026-07-28 | Expose explicit run closure and make proof scope part of proof identity | Owner cancellation and terminal status must be reachable through the documented controller, while intermediate work must not overwrite a full-suite record | Leave `RunLedger.close` unreachable; preserve one proof key and refuse every later intermediate write |
| 2026-07-28 | Record the observed mid-task disappearance of the host-supplied `PLUGIN_ROOT` contract as a separate Codex host/bootstrap defect and preserve fail-closed bootstrap behavior | Without a host-supplied absolute plugin root, the inline verifier cannot authenticate the one-file hook runtime before execution. A later trusted `apply_patch` probe succeeded, so the transient environment loss is neither a v2 proof-identity defect nor justification for a weaker fallback | Search the repository, plugin cache, or `PATH` for a fallback root; run an unauthenticated runtime; silently allow the tool call; attribute the host defect to v2 fingerprinting |
| 2026-07-28 | Frame every v2 tree field with an unsigned 64-bit big-endian byte length | Delimiter-only serialization is ambiguous because Git blob bytes may contain NUL and impersonate later entry boundaries without a SHA-256 collision | Keep NUL delimiters; assume blob content cannot contain the delimiter |
| 2026-07-29 | Pin Git trust at the delegated-process and internal-helper boundaries without writing user configuration | Live final verification proved the Windows read-only sandbox SID differs from the repository owner and ordinary Git fails `dubious ownership`; inherited shell variables alone cannot fix helpers that intentionally strip `GIT_*` | Ask the verifier to mutate global `safe.directory`; keep encoding workarounds in prompts; weaken the read-only boundary |
| 2026-07-29 | Make the read-only verifier audit the exact-fingerprint suite proof instead of duplicating tests that require temporary writes | The final full suite already proves test execution on the frozen candidate, while native-Windows read-only verification correctly denied pytest temporary files; duplicating tests adds cost without an independent permission boundary | Give the verifier workspace-write; treat unavailable duplicate tests as implementation failure |
| 2026-07-29 | Enforce final-proof admission in the parent runner while preserving the proof store's unauthenticated status | Peer review correctly found that prompt-only proof review allowed PASS without a code-enforced exact-record precondition; structural admission can be necessary without being misrepresented as provenance attestation | Let the model decide whether it consulted a proof; call a user-writable record independent attestation; rerun pytest with repository write permission |
| 2026-07-29 | Limit subprocess-environment claims to requested argv shape plus live point-in-time evidence | Repository tests prove the exact CLI override, but not Codex table merging or `inherit="core"` membership | State that the child receives only the supplied map on every Codex build |
| 2026-07-29 | Construct every proof-identity Git command, including `cat-file --batch`, through one canonical root-pinned argv helper | The final permission-isolated verifier proved the direct helper was pinned but the independent batch-object subprocess was not; the old regression exercised only `rev-parse` and allowed the internal path to fail under the sandbox SID | Add another environment workaround in the verifier prompt; mutate global Git configuration; accept direct-helper coverage as proof of a different subprocess |
| 2026-07-29 | Reopen this packet at plan revision 2 instead of opening another packet | The first slice completed its stated primitive hardening but the integrated dogfood proved the usability objective remained unmet; a second active packet or recovery plan would worsen lineage ambiguity | Preserve 44/44 as current truth; create a new control-plane packet; continue ad hoc recovery prompts |
| 2026-07-29 | Keep one current structured plan per Owner objective and retain older revisions only as explicitly superseded history | Multiple prose plans and recovery instructions made it unclear which contract governed the next action | Delete old evidence; let the pilot choose among stored plans; treat the newest timestamp as authority |
| 2026-07-29 | Move Owner authority and push scope into a strict local manifest that the peer does not negotiate | Three recent peer blockers came from contradictory or widened authorization prose created by the pilot, not from the technical change | Ask Claude to interpret transcript authority; embed authorization verbatim in executable packet evidence |
| 2026-07-29 | Make one controller state machine own phase order and executor admission | The current CLI exposes safe primitives but leaves the LLM to coordinate them manually, which made agent reasoning part of the safety-critical control plane | Add more operator prose; rely on the pilot to remember the 505-line plan |
| 2026-07-29 | Add an objective-level circuit breaker across durable run IDs | Run-local envelopes stopped individual calls but explicit new runs restarted the same pre-worker failure pattern indefinitely | Lower every call budget; allow each Owner continuation to reset all counters |
| 2026-07-29 | Preserve the existing safety gates while compressing task-specific plans | The one-line dogfood task accumulated about 46 times more plan/evidence bytes than mutation bytes because reusable controls were restated as bespoke prose | Remove verification or peer review; accept the large plan as the cost of safety |
| 2026-07-29 | Add a named-environment fallback for workflow payloads while keeping stdin primary | Two local Codex-shell attempts delivered empty stdin to the child before workflow or provider creation; passing only a validated variable name in argv preserves the no-payload-in-argv boundary | Start a third identical pipeline attempt; put manifest JSON directly in argv; require a temporary repository file |
| 2026-07-29 | Bind the compact structured plan to the exact current bytes of the packet's sole `plan.md` and recheck at every admission | An out-of-tree current-plan pointer alone could drift from the human-readable source of truth the Owner inspects | Trust matching revision numbers; copy the full packet plan into local state; select the newest timestamp |
| 2026-07-29 | Put objective-circuit thresholds in the strict manifest | The spec promised configurable defaults while the first implementation used module constants directly | Keep hidden constants; let the peer choose thresholds; allow plan text to override them |
| 2026-07-29 | Anchor objective identity outside editable plan prose and consume Owner actions once | Opus round one proved that a prose-derived objective digest, replayable action digest, and arbitrary digest change could recreate the repeated-recovery loop under new names | Treat renamed prose as a new objective; allow an Owner continuation to reset the circuit; call any changed digest material |
| 2026-07-29 | Bound the complete objective, including post-worker retries, with one maximum circuit resolution | A worker start otherwise launders accumulated time/spend and mutation-review cycles can continue indefinitely | Count only pre-worker procedural failures; reset on successful spawn; permit unlimited Owner-approved closes |
| 2026-07-29 | Use official-executor single-use admissions inside an explicitly same-user TCB and keep push unavailable | A local user-writable record cannot honestly be called signed hostile-user enforcement, but gate satisfaction still needs an executor-side consume boundary | Claim raw primitive invocation is prevented; leave admissions advisory; expose push before a dedicated wrapper exists |
| 2026-07-29 | Make insufficient context a first-class non-converging technical verdict | Delta-only review otherwise pressures a peer to decide without the dependencies only that peer can identify | Treat missing context as a procedural failure; infer convergence from an empty blocker list after truncation |
| 2026-07-29 | Make the runner create the isolated candidate commit before cross-review and keep Owner integration separate | Cross-review, proof, and verification need one stable committed identity, while the worker must not receive Git-metadata authority and an isolated commit does not move the Owner branch | Review an uncommitted tree and change identity before proof; let the worker commit; combine candidate creation with Owner integration |
| 2026-07-29 | Integrate only through an admission-bound fast-forward wrapper and make ambiguous failure terminal | Exact pre/post fingerprints allow a clean local integration without adding push authority; automatic retry is safe only when the complete Owner preimage is positively unchanged | Invoke raw merge from the pilot; retry after an unconfirmed partial update; roll back destructively |
| 2026-07-29 | Audit worktree-local Git config only when `extensions.worktreeConfig` is enabled | The actual Windows regression proved `git config --worktree` itself fails once a linked worktree exists unless that extension is active | Treat the command failure as absence; skip all local configuration checks; enable the repository extension as a side effect |
